# 의존성/빌드 산출물 — 이미지 안에서 새로 생성
node_modules
.next
out
build

# codegen 산출물 — 빌드 시 vendored 스키마로 재생성 (./schema.graphqls 는 포함)
src/gql

# git / 메타
.git
.gitignore

# 시크릿 — NEXT_PUBLIC_* 는 build arg, 서버 env 는 compose 로 주입
.env*

# 로컬/에디터/로그
*.log
.DS_Store
.vscode
.idea

# Docker 자체
Dockerfile
.dockerignore
docker-compose*.yml
